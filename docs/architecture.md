# Архитектура

MVP работает без сервера: `index.html` задаёт семантическую структуру, `styles.css` — визуальную систему, `app.js` — состояние карты и взаимодействия. Архитектура допускает последующую замену локального советника API-модулем и добавление Supabase без изменения интерфейса.
